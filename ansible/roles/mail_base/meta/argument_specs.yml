---
argument_specs:
  main:
    short_description: Mail stack packages, vmail user and DNSSEC resolver for the mail host
    options:
      mail_hostname:
        type: str
        required: true
      mail_vmail_uid:
        type: int
        required: true
      mail_vmail_home:
        type: str
        required: true
      mail_rspamd_upstream_suite:
        type: str
        required: true
