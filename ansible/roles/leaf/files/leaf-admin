#!/bin/sh
set -eu

[ "$(id -u)" -eq 0 ] || { echo "run this with sudo" >&2; exit 1; }

set -a
. /etc/leaf/server.env
set +a
exec setpriv --reuid leaf --regid leaf --init-groups /opt/leaf/bin/admin "$@"
